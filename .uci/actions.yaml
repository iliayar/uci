actions:
  deploy_manual:
    - on: call
      run_pipelines:
        - deploy
  deploy:
    - on: changed
      repo_id: uci
      changes:
        - '.*' # All files
      run_pipelines:
        - deploy
