pipelines:
  echo: ${load(./pipelines/echo_pipeline.yaml)}
  test-needs: ${load(./pipelines/test_pipeline.yaml)}
  stub: ${load(./pipelines/stub_pipeline.yaml)}
  test_cancel: ${load(./pipelines/test_cancel.yaml)}
  test_web: ${load(./pipelines/test_web.yaml)}
