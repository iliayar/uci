actions:
  main:
    - on: call
      run_pipelines:
        - echo
  deploy:
    - on: call
      services:
        test-service: deploy
        common-service: deploy

  test:
    - on: call
      run_pipelines:
        - test-needs
