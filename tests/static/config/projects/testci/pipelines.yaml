pipelines:
  echo:
    path: pipelines/echo_pipeline.yaml
  test-needs:
    path: pipelines/test_pipeline.yaml
  stub:
    path: pipelines/stub_pipeline.yaml
  test_cancel:
    path: pipelines/test_cancel.yaml
